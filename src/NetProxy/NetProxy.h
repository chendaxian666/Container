#ifndef NETPROXY
#define NETPROXY

/// @brief base net class,offer capacity to communicate.
class NetProxy
{
private:
    /* data */
public:
    NetProxy(/* args */);
    ~NetProxy();
};

NetProxy::NetProxy(/* args */)
{
}

NetProxy::~NetProxy()
{
}

#endif

